# 数据库配置
# Docker环境下使用网关IP访问宿主机MySQL
# 在不同环境中，这个IP可能需要调整：
# - Linux Docker: 172.25.0.1 (网关IP)
# - Docker Desktop (Windows/Mac): host.docker.internal
# - 本地开发: localhost
DB_HOST=172.25.0.1
DB_USER=linux_db
DB_PASSWORD=root
DB_ROOT_PASSWORD=root
DB_NAME=ipv6_project

# 应用配置
NODE_ENV=production
FRONTEND_PORT=5173
BACKEND_PORT=3000

# 安全配置
JWT_SECRET=jwt_secret_key_for_ipv6_project_2025_very_secure

# 工具路径配置 (容器内路径)
XMAP_PATH=/usr/local/bin/xmap
ZGRAB2_PATH=/usr/local/bin/zgrab2

# 项目根目录 (容器内路径)
PROJECT_ROOT=/app

# 日志级别
LOG_LEVEL=info

# 文件上传限制
MAX_FILE_SIZE=50mb

# 扫描配置
DEFAULT_SCAN_RATE=2000
DEFAULT_MAX_RESULTS=50000
